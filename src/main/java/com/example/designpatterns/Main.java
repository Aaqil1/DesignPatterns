package com.example.designpatterns;

/**
 * Entry point explaining how to run individual pattern demos.
 * The real behavior for each pattern lives in its own package-specific Main class.
 */
public class Main {
    public static void main(String[] args) {
        System.out.println("Run a specific pattern demo via its Main class, e.g.:");
        System.out.println("mvn -DskipTests compile exec:java -Dexec.mainClass=\"com.example.designpatterns.creational.singleton.Main\"");
    }
}

